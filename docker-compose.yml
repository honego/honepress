services:
  blog:
    build: .
    container_name: blog
    restart: unless-stopped
    ports:
      - 8080:8080
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml
    network_mode: bridge
