services:
  honepress:
    build: .
    container_name: honepress
    restart: unless-stopped
    ports:
      - 8080:8080
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml
    network_mode: bridge
